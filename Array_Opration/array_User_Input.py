# Get the size of the array from the user
n = int(input("enter the size of array"))

# Initialize an empty array
arr = []

# Loop through the range of the array size
for i in range(0, n):
    # Get each number from the user and store it in the array
    e = int(input("enter the number"))

# Print the array
print(arr)